[project]
name = "feel-the-distribution"
version = "0.1.0"
description = "AI Systems Engineering, Week 1 lab: one fixed input, one hundred calls, one distribution."
requires-python = ">=3.12"
dependencies = [
    "anthropic>=1.0",
    "matplotlib>=3.9",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
