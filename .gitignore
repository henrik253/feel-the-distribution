# Secrets — the Anthropic key lives in .env and never in a commit
.env

# Run outputs (records, plots). Keep your own; don't commit them.
runs/

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.DS_Store
